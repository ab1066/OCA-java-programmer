package com.chhitijbhattachan.tutorials.java.api.java_lang;
/**
 * two primitives are compared for equality using == (double equals to operator)
 * but in order to compare two objects for equality, there are two ways to do so
 * 1. using == (double equal operator)
 * 2. using .equals(Object o) method
 *      .equals(Object o) method states that if the hashCode of two objects are same then they are equal by default
 */
public class ExploringEqualsWithComparisonOperator{
    public static void main(String... args){
        
        

    }
}